<% provide(:title, "Deposit") %>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1> Deposit to an existing account here </h1></br>
        <%= render 'shared/user_info' %>
      </section>
      <section class="account_show">
        <%= render 'shared/account_show' %>
      </section>
    </aside>
  </div>
<% end %>
