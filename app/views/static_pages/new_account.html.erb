<% provide(:title, "New Account") %>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1> Open a new account here </h1><br/>
        <%= render 'shared/user_info' %>
      </section>
      <section class="account_form">
        <%= render 'shared/account_form' %>
      </section>
    </aside>
  </div>
<% end %>
